# sample-info

Sample info script. Windows-targeted example.

- Author: wangsyiiz
- Version: 0.1.0
- Platforms: windows
- Files: `install.ps1`

Usage (Windows PowerShell):

1. 打开 PowerShell，可能需要提升权限（以管理员身份运行）。
2. 若遇到执行策略限制，可临时允许运行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

3. 运行脚本：

```powershell
.\install.ps1
```

说明：此脚本为 Windows 示例，展示如何在 PowerShell 中获取/显示样例信息。